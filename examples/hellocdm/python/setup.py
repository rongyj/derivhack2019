from setuptools import setup

setup(name='hello-cdm',
      version='0.1',
      description='Hello CDM bot',
      install_requires=['dazl'],
      packages=['bot'],
      include_package_data=True)
